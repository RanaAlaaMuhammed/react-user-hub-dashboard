import React from 'react';

const Header = () => {
  return (
    <div>
        <h1 style={{ fontSize: '3rem', marginBottom: '0.5rem',fontStyle: 'italic' }}>User Hub</h1>
        <p style={{ fontSize: '1.25rem', fontStyle: 'italic', color: 'rgb(207 207 207)' }}>
            "Your one-stop hub to find, manage, and connect with every user!"
        </p>
    </div>
  );
};

export default Header;
